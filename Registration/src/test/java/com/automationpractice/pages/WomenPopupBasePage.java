package com.automationpractice.pages;

import org.apache.logging.log4j.Logger;
import org.openqa.selenium.WebDriver;

public class WomenPopupBasePage extends HeaderPageObject {
    public WomenPopupBasePage(WebDriver driver, Logger log) {
        super(driver, log);
    }
}
